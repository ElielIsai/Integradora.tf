pipeline {
    agent any

    environment {
        // Mismas credenciales de AWS
        AWS_ACCESS_KEY_ID     = credentials('AWS_ACCESS_KEY_ID')
        AWS_SECRET_ACCESS_KEY = credentials('AWS_SECRET_ACCESS_KEY')
    }

    stages {
        stage('Obtener Código') {
            steps {
                deleteDir()
                git branch: 'main', 
                    credentialsId: 'ACCESO_REPO', 
                    url: 'https://github.com/ElielIsai/Integradora.tf.git'
            }
        }

        stage('Terraform Destroy') {
            steps {
                input message: '¿Borrar todo?', ok: 'Sí'
                // El comando -refresh=true obliga a Terraform a mirar AWS antes de intentar borrar
                sh 'terraform init -reconfigure' 
                sh 'terraform destroy -auto-approve -refresh=true'
            }
        }
    }

    post {
        always {
            echo "Proceso de destrucción finalizado."
        }
    }
}